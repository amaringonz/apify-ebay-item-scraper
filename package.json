{
    "name": "ebay-item-scraper",
    "version": "1.2.0",
    "description": "Apify actor that extracts full product data from a single eBay item page.",
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "apify": "^3.7.2",
        "crawlee": "^3.17.0",
        "playwright": "^1.47.0"
    },
    "devDependencies": {
        "@apify/tsconfig": "^0.1.0",
        "@semantic-release/changelog": "^7.0.0",
        "@semantic-release/exec": "^7.1.0",
        "@semantic-release/git": "^11.0.1",
        "@types/node": "^20",
        "cheerio": "1.0.0-rc.12",
        "semantic-release": "^25.0.8",
        "typescript": "^5.5"
    },
    "scripts": {
        "start": "npm run build && node dist/main.js",
        "start:dev": "tsc && node dist/main.js",
        "build": "tsc",
        "release": "semantic-release"
    }
}
